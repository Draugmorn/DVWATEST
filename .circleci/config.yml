version: 2
jobs:
 build:
   machine: true
   steps:
     - checkout
     - run:
         docker build -f https://github.com/Draugmorn/DVWATEST/ - < Dockerfile
     - run:
          name: The First Step
          command: docker run -dit -p 80:80 --name www $id

